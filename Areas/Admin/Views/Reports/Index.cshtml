@{
    ViewData["Title"] = "Звіти та експорт";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <h1 class="mb-4"><i class="fas fa-file-export"></i> Звіти та експорт даних</h1>

    <div class="row g-4">
        <!-- Export Orders -->
        <div class="col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-shopping-cart"></i> Експорт замовлень</h5>
                </div>
                <div class="card-body">
                    <p class="card-text">Завантажте звіт по замовленням за вибраний період</p>
                    <form asp-action="ExportOrders" method="post">
                        <div class="mb-3">
                            <label class="form-label">Дата від:</label>
                            <input type="date" name="from" class="form-control" 
                                   value="@DateTime.Today.AddMonths(-1).ToString("yyyy-MM-dd")" 
                                   max="@DateTime.Today.ToString("yyyy-MM-dd")">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Дата до:</label>
                            <input type="date" name="to" class="form-control" 
                                   value="@DateTime.Today.ToString("yyyy-MM-dd")" 
                                   max="@DateTime.Today.ToString("yyyy-MM-dd")">
                        </div>
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-download"></i> Завантажити Excel
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Export Products -->
        <div class="col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-boxes"></i> Експорт товарів</h5>
                </div>
                <div class="card-body">
                    <p class="card-text">Завантажте повний список товарів з базі даних</p>
                    <div class="mb-3">
                        <ul class="list-unstyled">
                            <li><i class="fas fa-check text-success"></i> Назва товару</li>
                            <li><i class="fas fa-check text-success"></i> Категорія</li>
                            <li><i class="fas fa-check text-success"></i> Ціна та залишки</li>
                            <li><i class="fas fa-check text-success"></i> Рейтинг</li>
                        </ul>
                    </div>
                    <form asp-action="ExportProducts" method="post">
                        <button type="submit" class="btn btn-success w-100">
                            <i class="fas fa-download"></i> Завантажити Excel
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Export Customers -->
        <div class="col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="fas fa-users"></i> Експорт клієнтів</h5>
                </div>
                <div class="card-body">
                    <p class="card-text">Завантажте базу клієнтів з контактною інформацією</p>
                    <div class="mb-3">
                        <ul class="list-unstyled">
                            <li><i class="fas fa-check text-info"></i> ПІБ</li>
                            <li><i class="fas fa-check text-info"></i> Контакти</li>
                            <li><i class="fas fa-check text-info"></i> Кількість замовлень</li>
                            <li><i class="fas fa-check text-info"></i> Дата реєстрації</li>
                        </ul>
                    </div>
                    <form asp-action="ExportCustomers" method="post">
                        <button type="submit" class="btn btn-info w-100">
                            <i class="fas fa-download"></i> Завантажити Excel
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Statistics Overview -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0"><i class="fas fa-chart-bar"></i> Інформація про звіти</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h6>Формат файлів</h6>
                            <p>Всі звіти експортуються у форматі Microsoft Excel (.xlsx), який можна відкрити в Excel, Google Sheets або інших програмах для роботи з таблицями.</p>
                        </div>
                        <div class="col-md-6">
                            <h6>Рекомендації</h6>
                            <ul>
                                <li>Регулярно створюйте резервні копії даних</li>
                                <li>Аналізуйте звіти для покращення продажів</li>
                                <li>Використовуйте фільтри в Excel для детального аналізу</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Statistics -->
    <div class="row mt-4 g-4">
        <div class="col-md-3">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <i class="fas fa-file-excel fa-3x text-success mb-3"></i>
                    <h6>Формат Excel</h6>
                    <p class="small text-muted mb-0">Легко редагувати та аналізувати</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <i class="fas fa-filter fa-3x text-primary mb-3"></i>
                    <h6>Гнучкі фільтри</h6>
                    <p class="small text-muted mb-0">Виберіть потрібний період</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <i class="fas fa-chart-line fa-3x text-warning mb-3"></i>
                    <h6>Аналітика</h6>
                    <p class="small text-muted mb-0">Детальна статистика</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm text-center">
                <div class="card-body">
                    <i class="fas fa-clock fa-3x text-info mb-3"></i>
                    <h6>Швидко</h6>
                    <p class="small text-muted mb-0">Миттєве завантаження</p>
                </div>
            </div>
        </div>
    </div>
</div>